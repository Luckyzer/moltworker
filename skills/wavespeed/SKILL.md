# WaveSpeed Image Generation

Use this skill to generate images from text prompts using WaveSpeed AI.

## When to use
- User asks to generate/create an image

## Requirements
- WAVESPEED_API_KEY must be set in the environment

## Usage
node /home/openclaw/clawd/skills/wavespeed/wavespeed-client.js generate "<prompt>"
